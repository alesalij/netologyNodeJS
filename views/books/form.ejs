<form method="POST">
  <!-- <%   Object.keys(book).forEach((key,index)=>{
    if(index<Object.keys(book).length-1){
 %>
  <div class="row mb-3">
    <label class="col-sm-2 col-form-label"><%= key %>:</label>
    <div class="col-sm-10">
      <input
        type="form-text"
        class="form-control"
        id="input<%= key %>"
        name="<%= key %>"
        value="<%= book[key] %>"
      />
    </div>
  </div>
  <%} }) %> -->
  <div class="row mb-3">
    <label class="col-sm-2 col-form-label">fff</label>
    <div class="col-sm-10">
      <input
        type="form-text"
        class="form-control"
        id="inputName"
        name="fff"
        value="ffhhhf"
      />
    </div>
  </div>
  <!-- <div class="row mb-3">
    <label class="col-sm-2 col-form-label">fileBook:</label>
    <div class="col-sm-10">
      <input type="file" class="form-control-file" id="inputfileBook"
      name="fileBook" value="<%= book["fileBook"] %>" />
    </div>
  </div>-->
  <button type="submit" class="btn btn-primary"><%= typeForm%></button>
</form>
